libstd_srcs = files(
    'std_string.c',
    'std_sprintf.c',
    'std_unicode.c'
)

if(sdk_version_major == 5)
    libstd_srcs += files(
        'std_stdlib.c',
        'std_unicode_array.c'
    )
endif
