libdemo_srcs = files(
    'DEMOInit.c',
    'DEMOKey.c',
    'DEMOUtility.c'
)

if(sdk_version_major == 5)
    libdemo_srcs += files(
        'DEMOBitmap.c',
        'DEMOFont.c',
        'DEMOHostio.c',
        'DEMOWave.c'
    )

endif
